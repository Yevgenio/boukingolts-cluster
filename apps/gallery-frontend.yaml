apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gallery-frontend
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Yevgenio/boukingolts-cluster
    targetRevision: HEAD
    path: manifests/gallery-frontend
    helm:
      releaseName: gallery-frontend
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: gallery-frontend
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
